#pragma once
#include <Command.h>

class SketchCommand : public Command {
public:
    class SketchCommandId {
    public:
        enum Type : u32 {
            Line,
        };

        Type type;
        u32 index;
    };


    explicit SketchCommand() = default;
};